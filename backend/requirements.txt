# ── Backend Runtime Bağımlılıkları ─────────────────────────────────────────
# Bu dosya backend/ servisinin çalışması için gereken paketleri listeler.
# Root requirements.txt quant-engine'e özgü ağır paketleri (polars, numba vb.) içerir;
# bu dosya sadece web-gateway katmanını kapsar.

# Web Framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Rate limiting
slowapi>=0.1.9

# Auth & Security
argon2-cffi>=23.1.0
python-jose[cryptography]>=3.3.0
pyotp>=2.9.0
email-validator>=2.1.0

# Database
sqlalchemy>=2.0.0
pymysql>=1.1.0
aiomysql>=0.2.0
aiofiles>=23.2.1

# Payments
stripe>=8.0.0

# Monitoring
sentry-sdk[fastapi]>=2.0.0

# Config
pydantic>=2.6.0
pydantic-settings>=2.2.0

# HTTP Client
httpx>=0.27.0

# Notifications
loguru>=0.7.2

# Data (shared with quant-engine — pinned to same range)
redis>=5.0.0
websockets>=12.0

# Test (only needed in CI)
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
