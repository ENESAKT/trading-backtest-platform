# === Quant Engine — Bağımlılıklar ===

# Veri İşleme
polars>=1.0.0
pandas>=2.1.0
numpy>=1.26.0

# JIT Compiler (C-seviyesi hız)
numba>=0.59.0

# Veritabanı (Embedded Columnar DB)
duckdb>=1.0.0
pyarrow>=15.0.0

# Veri Çekme (Data Pipeline)
ccxt>=4.3.0
yfinance>=0.2.36

# Teknik İndikatörler
# pandas-ta kaldırıldı (pip'te bulunamıyor). İndikatörler ileride kendimiz yazılacak.

# Konfigürasyon Yönetimi
pydantic>=2.6.0
pydantic-settings>=2.2.0
tomli>=2.0.0; python_version < "3.11"

# CLI Arayüzü
typer>=0.12.0
rich>=13.7.0

# Raporlama ve Görselleştirme
plotly>=5.18.0
jinja2>=3.1.0
kaleido>=0.2.1
lightweight-charts>=2.1.0

# Optimizasyon
optuna>=3.5.0

# Loglama
loguru>=0.7.2

# Web Gateway (FastAPI + ASGI)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Test
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0  # async worker testleri için
httpx>=0.27.0  # FastAPI TestClient için
websockets>=12.0  # Binance public stream daemon (uvicorn[standard] zaten getirir, açık declare)

# Geliştirme
ruff>=0.3.0
